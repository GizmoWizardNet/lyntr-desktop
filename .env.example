# Base URL of the real, shared Lyntr backend. The desktop app talks to this
# directly — there's no separate desktop API. WebSocket URL is derived from
# this automatically (https -> wss, /ws path) in src/lib/api/config.ts.
VITE_LYNTR_API_URL=https://lyntr.gizmowizard.tech
